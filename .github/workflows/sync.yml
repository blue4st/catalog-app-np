name: Sync catalog-app-np via SSH

on:
  push:
    branches:
      - main  # change if your main branch has a different name

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo A
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # fetch all history for mirror push

      - name: Setup SSH key
        uses: webfactory/ssh-agent@v0.8.1
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add SSH remote for Repo B
        run: |
          git remote remove catalog-app-np || true
          git remote add catalog-app-np git@github.com:blue4st/catalog-app-np.git
          echo "✅ Remote added via SSH"

      - name: Mirror push to Repo B
        run: |
          echo "🔄 Starting mirror push to catalog-app-np"
          git fetch catalog-app-np || true
          git push --mirror catalog-app-np
          echo "✅ Mirror push complete"
